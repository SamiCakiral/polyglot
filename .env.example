# =============================================================================
# Flask
# =============================================================================
SECRET_KEY=change-me-in-production
DATABASE_URL=sqlite:///anki.db

# =============================================================================
# LLM Backend
# =============================================================================
# Backend principal : lmstudio | vllm | ollama
LLM_BACKEND=lmstudio
LLM_TIMEOUT=120

# LM Studio (serveur principal)
LMSTUDIO_HOST=localhost
LMSTUDIO_PORT=1234
LMSTUDIO_MODEL=openai/gpt-oss-20b

# vLLM / Ollama (fallback sur serveur GPU)
SPARK_HOST=localhost
VLLM_PORT=8000
OLLAMA_PORT=11434

# =============================================================================
# TTS (Qwen3-TTS)
# =============================================================================
TTS_PORT=9100
